global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 3h
  receiver: discord

receivers:
  - name: discord
    webhook_configs:
      - url: http://alertmanager-discord:8000/alertmanager
        send_resolved: true
